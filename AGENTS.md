# Repository Agent Notes

<!-- REPO-LOCAL-SKILLS:START -->
## Repo-local Skills

這個 repo 不再維護一整套自寫 workflow skills。一般開發、修復、review、測試與交付流程，優先使用使用者層級的 superpowers skills。

### 保留的 repo-local skills

- `domain-understanding`：只在需要理解專案特定領域規則、角色、權限、狀態轉換、款項流動或跨角色流程時使用。
- `ui-prototype`：只在需要把專案特定 UI / UX 想法整理成可 review 的實際前端切版或 prototype 時使用。

### `.ai` artifacts

- 舊 `.ai` workflow artifacts 會整理成「一個需求一個 `.md`」的精簡紀錄後再刪除。
- `.ai/domain/` 是長期領域資料，保留並可隨需求調整；不要當作一次性 workflow artifact 刪除。
- `.ai/domain-impact/` 是需求級 delta，若已收斂進 `.ai/requirements/`，可以刪除。
- 不再使用 `.ai/current-task.md` 或 `.ai/current-task.template.md`。
- 不再把 `.ai/workflow.md`、`.ai/project-context.md` 當作新任務的必讀入口。
<!-- REPO-LOCAL-SKILLS:END -->
