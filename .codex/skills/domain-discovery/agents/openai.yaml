interface:
  display_name: "Domain Discovery"
  short_description: "Update durable domain models and domain impact"
  default_prompt: "Use $domain-discovery to update domain artifacts for this intent."
policy:
  allow_implicit_invocation: true
